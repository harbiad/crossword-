import {readFileSync,writeFileSync,existsSync}from'node:fs';
import {createHash}from'node:crypto';
const root='dictionary/stage3b/batch006_analysis/',json=p=>JSON.parse(readFileSync(p,'utf8'));
const s=json(root+'summary.json'),runs=json(root+'runs.json'),selected=json(root+'proposed_candidates.json');
for(const [file,hash]of Object.entries(json(root+'inputs.json')))if(createHash('sha256').update(readFileSync(file)).digest('hex')!==hash)throw Error(`Protected input changed: ${file}`);
// This CSV has scalar fields only; distinguish runs with actual zeros from nonzero risk observations.
const [header,...records]=readFileSync(root+'failure_classification.csv','utf8').replace(/^\uFEFF/,'').trim().split(/\r?\n/).map(line=>line.split(',').map(x=>x.replace(/^"|"$/g,'')));
const classified=records.map(values=>Object.fromEntries(header.map((key,i)=>[key,values[i]])));
const zeroRuns=rs=>Object.fromEntries(['A_TRUE_GAP','B_USED_EXHAUSTION','C_API_SAMPLING_LOSS'].map(type=>[type,rs.filter(r=>r.type===type&&Number(r.zeroObservations)>0).length]));
s.actualZeroRuns=zeroRuns(classified);
for(const c of s.configurations)c.actualZeroRuns=zeroRuns(classified.filter(r=>Number(r.size)===c.size&&r.mode===c.mode));
const hashes=Object.fromEntries(runs.map(r=>[r.file,createHash('sha256').update(readFileSync(root+r.file)).digest('hex')]));
if(existsSync(root+'trace_hashes.json')&&JSON.stringify(json(root+'trace_hashes.json'))!==JSON.stringify(hashes))throw Error('Archived trace changed');
writeFileSync(root+'trace_hashes.json',JSON.stringify(hashes,null,2)+'\n');
const mode=m=>m==='ar_to_en'?'AR→EN':'EN→AR',f=n=>n.toLocaleString('en-US');
const recommendation={choice:'A',scope:`Targeted AR→EN linguistic review of the proposed ${selected.length} headwords is the main next step for the two highest-priority English-answer grids.`,sampling:'Sampling need not be fixed before that English-focused review: no API pool loss was observed for either English configuration. Investigate Arabic sampling separately before commissioning more Arabic-focused review; existing approved matches should not be replaced by needless dictionary work.',caveat:'No counterfactual full-pool solver experiment was run. Branch-domain counts cannot identify what fraction of entire puzzles a sampling or dictionary change would rescue. Used-word exhaustion also depends on earlier search choices; search efficiency remains a possible joint limitation.',reviewStarted:false};
s.recommendation=recommendation;writeFileSync(root+'summary.json',JSON.stringify(s,null,2)+'\n');
const lines=['# Stage 3B: residual-domain analysis before Batch006','','**Analysis and proposed selection only.** No relationship was linguistically reviewed, approved, rejected, corrected or assigned CEFR. The 5,655 approved relationships and production compatibility policy are unchanged. Generator source, sampling algorithm, budgets, inversion and duplicate-answer rules are unchanged; protected input hashes are verified.','','## Evidence and capture','','Re-ran every failed seed from the Batch005 approved-only benchmark for the five requested configurations: **81 calls, all 81 still failed**, advanced band, the original seeds within 1–30, unchanged production pool limits and generator options. No already-ready configuration was rerun. These are failure diagnostics, not a new success-rate benchmark.','','The benchmark-only Vite observer inserts logging into in-memory modules. Every solver attempt records its template, candidate window, placement count and observation counters. Capture takes the first 128 distinct slot/support-set/used-word/reason records **per attempt**, avoiding the previous run-wide cap that concentrated on the first template. Raw calls after the cap are counted, not silently presented as captured distinct states.', '',`Observed ${f(s.solverAttempts)} solver attempts; ${f(s.constructNoPlacements??0)} returned no placements. ${f(s.useful.observations)} useful records contain ${f(s.useful.zeroObservations)} actual zero-domain observations and ${f(s.useful.criticallySmall)} nonzero 1–3-candidate observations. ${f(s.excludedTruncated)} truncated MRV probes were excluded after recomputing the actual window/used-word domain. ${f(s.uncapturedCalls)} calls were uncaptured; these may repeat and are not unique-domain counts.`, '', 'Observer overhead affects wall-clock deadlines and search trajectories. All 81 failure outcomes reproduced, but timing and attempt-count comparisons with uninstrumented runs would be invalid. These bounded observations are not unbiased samples of all search states.','','## Classification','','Counts below are **captured zero-domain observations**, not independent failed puzzles or percentages of failures causally attributable to a category. A run can encounter several categories.','','| Grid | Mode | Failed runs | A: full approved gap | B: used-word exhaustion | C: API sampling loss |','|---|---|---:|---:|---:|---:|'];
for(const c of s.configurations)lines.push(`| ${c.size} | ${mode(c.mode)} | ${c.stillFailed} | ${f(c.byType.A_TRUE_GAP?.zeroObservations??0)} | ${f(c.byType.B_USED_EXHAUSTION?.zeroObservations??0)} | ${f(c.byType.C_API_SAMPLING_LOSS?.zeroObservations??0)} |`);
lines.push('',`Total actual zeros: **${f(s.classification.A_TRUE_GAP.zeroObservations)} A / ${f(s.classification.B_USED_EXHAUSTION.zeroObservations)} B / ${f(s.classification.C_API_SAMPLING_LOSS.zeroObservations)} C**. Actual A zeros occur in 78/81 runs; B in 70; C in 22. Including nonzero risk states, A appears in 81 and C in 25. The table is stratified because English search produced far more observed states; pooling percentages would hide the Arabic sampling effect.`, '', 'A means no full eligible approved canonical answer matches the recorded support constraints. B means matching answers exist but all are already in the used-word set. C means at least one unused full-set answer exists but none survives the API sample. Internal candidate-window and propagation-only restrictions have separate labels rather than being misclassified as sampling; no such zero cases were captured in this run.', '', 'A live arc domain can still contain 1–3 oriented candidates while recomputation against all current neighbor support sets yields zero: propagation is mid-update. These nonzero intermediate states are retained as risk observations, not counted as actual zero failures. Supported small domains are likewise not falsely called A/B/C failures.', '', 'Pattern positions follow normal traversal; inversion translates the position to length−1−position without reversing stored answers. A question mark can hide a multi-character support set: use the constraints column. Equivalent normal/inverted constraint sets are canonicalized for domain ranking, while raw observations retain original slot coordinates and direction.', '', 'Full matches use the size-specific eligible approved index and distinct canonical grid answers, not relationship counts. Source English-length eligibility still applies to Arabic-answer candidates. Different Arabic clues for the same English answer, or new relationships with an existing Arabic grid spelling, do not count as additional diversity.','','## Sampling and used-word diversity','','| Grid/mode | Runs with full>0, API=0 | Zero observations caused by API loss | Full≥5 → API 1–3 observations |','|---|---:|---:|---:|');
for(const c of s.configurations)lines.push(`| ${c.size} ${mode(c.mode)} | ${c.samplingZero.runs}/${c.stillFailed} | ${c.byType.C_API_SAMPLING_LOSS?.zeroObservations??0} | ${c.samplingHealthyToSmall.observations} |`);
lines.push('', 'Arabic 11×11: 69/262 captured zeros (26.3%) are API sample loss. Arabic 13×13: 24/238 (10.1%) are sample loss; 160/238 (67.2%) are used-word exhaustion. Arabic 9×9: 5/136 (3.7%) are sample loss. These are diagnostic proportions, not predicted puzzle-success improvements. Both English configurations retained all relevant approved matches; changing API sampling cannot repair their measured missing-support or exhausted-answer cases.', '', `Full-domain-to-sample loss occurs in ${f(s.samplingLoss.observations)} captured records. ${s.samplingZero.observations} have full>0/sample=0 (98 actual zeros), and ${s.samplingHealthyToSmall.observations} shrink from at least five to 1–3. sampling_losses.csv lists full/sample/window counts, percentage retained and every omitted canonical answer.`, '', 'The most repeatedly exhausted English answers include '+s.exhaustedAnswers.slice(0,8).map(x=>`${x.answer} (${x.runs} runs)`).join(', ')+'. used_word_exhaustion.csv contains only actual zero cases and names the used matches; raw archives preserve all earlier choices. Additional distinct valid answers could help; another clue for the same canonical answer cannot.', '', '| Full matching canonical answers before consumption | Exhaustion zero observations | Runs affected |','|---|---:|---:|',...s.exhaustionDiversity.map(x=>`| ${x.fullDomainSize} | ${x.zeroObservations} | ${x.runs} |`),'','## Configuration assessment','');
for(const c of s.configurations){const useful=c.zeroByLength.filter(x=>x.trueGaps||x.usedExhaustion||x.samplingLoss);lines.push(`**${c.size} ${mode(c.mode)}:** observed zero constraints involve lengths ${useful.map(x=>x.length).join(', ')}. Full approved index: ${c.fullEligibleUnique} distinct eligible answers. These are observed pattern lengths, not recommended quotas.`);}
lines.push('', '**13×13 AR→EN remains 0%:** all 30 failed calls contain unsupported crossing domains and used-word exhaustion, with no API sampling loss. Repeatedly consumed full-grid answers indicate limited distinct alternatives for the chosen crossing states. **11×11 AR→EN remains 26.7%:** all 22 failed calls likewise have full-set gaps and exhaustion, without API loss. No trace proves that every alternative search branch is unsatisfiable.', '', '**13×13 EN→AR remains 50%:** the observed zeros are predominantly used-word exhaustion, with true gaps and sample loss also present. **11×11 EN→AR remains 63.3%:** 193 captured zeros lack full-set support and 69 lose existing approved support at the API boundary. Dictionary review must not be used to repair those latter cases. No generator/search change is justified solely by these traces.','','## Proposed review scope','','Ranking first requires A/B actual-zero recurrence in at least two seeds and nonempty support constraints. Weight = configuration priority (13 English=5, 11 English=4, 13 Arabic=3, 11 Arabic=2, 9 Arabic=1) × log2(1+distinct zero seeds) × log2(1+distinct templates) × 1 for A or 0.75 for B. This rewards repeated patterns without treating raw repeated calls as independent evidence.', '', 'Only new unresolved headwords from the common reference can enter. Stage3A and Batches001–005, prior deferrals, rejected/dialect relationships and directionally disallowed relationships are excluded. selection_exclusions.json adds **selection-only** name/brand/place/specialist deferrals; no dictionary classifications changed. Lexical words that also resemble names, such as LANCE, MESA and ATLAS, remain candidates. Learner score =100+80/(1+original dictionary index/2000); a reference Arabic form matching an existing relationship adds a 5% selection multiplier. These are disclosed proxies, not semantic or MSA judgments.', '', 'A greedy weighted-cover score multiplies newly covered domain weight by learner score and the reference-agreement hint. Already covered domains receive no further marginal credit. Stop at the first prefix covering 90% of **reachable weighted needs**. This is a review-cost planning threshold, not a proven optimum or a promise of approval/generation success.', '', `**Propose ${s.proposal.headwords} headwords**, from ${s.proposal.eligibleMatchingHeadwords} matching eligible headwords. They potentially cover ${s.proposal.coveredDomains}/${s.proposal.reachableDomains} reachable recurring domains (${s.proposal.weightedCoveragePercent.toFixed(2)}% weighted coverage). There are ${s.recurringRepairableTypeDomains} recurring A/B targets overall; ${s.proposal.unreachableDomains} have no candidate in the screened unreviewed reference pool. No blanket quota or 500-word default is used.`, '', '| Reachable weighted coverage | Headwords in greedy prefix |','|---|---:|',...s.proposal.alternatives.map(x=>`| ${x.targetPercent}% | ${x.headwords} |`),'','| Configuration | Recurring A/B targets | Reachable | Covered by proposed set |','|---|---:|---:|---:|',...s.proposal.byConfiguration.map(x=>`| ${x.size} ${mode(x.mode)} | ${x.recurringABTargets} | ${x.reachable} | ${x.selectedCoverage} |`),'', 'The proposed set is deliberately English-answer focused. It is not expected to repair Arabic residual domains. Only two recurring Arabic targets have screened potential candidates, and their low marginal weight does not place them in this 90% prefix. Do not infer that no Arabic vocabulary exists elsewhere; this is the current unresolved, never-previously-reviewed common-reference pool.', '', '**Exact proposed headwords, in selection order:**', '', selected.map(c=>c.english).join(', ')+'.', '', 'All current unresolved Arabic relationships, natural display text, answer lengths, matched support sets, seed/configuration contribution and score components are in proposed_candidates.csv / .json. No translation decisions or approvals were performed.','','## Recommendation','','**A — targeted English-answer dictionary review is the main next step.** '+recommendation.scope+' '+recommendation.sampling, '', recommendation.caveat, '', 'Before additional Arabic-focused review, investigate measured API losses using existing approved records. For the English proposal, linguistic quality remains the approval gate; if a candidate’s existing translation is wrong, leave it unresolved. Do not invent replacements to satisfy these domain estimates.','','## Artifacts and reproduction','','Required CSVs provide ranked domains, per-run failure classifications, sampling losses, exhaustion cases and proposals. observations.jsonl.gz is the lossless detailed classification archive; traces/*.json.gz retain every captured original observation, used-word list, template and candidate window. Compression avoids committing duplicate hundred-megabyte text files. Failure classification totals are not independent run counts.','','```sh','npm run bench:residual','node scripts/dictionary-residual-analysis.ts','node scripts/dictionary-residual-report.mjs','npm run dictionary:audit','npm test','npm run lint','npm run build','```','','Use Node 24 and installed dependencies. Preserve the current trace archive when experimenting: deadline-sensitive diagnostics may differ despite identical seeds. The report validates protected dictionary/generator hashes and refuses changed archived trace hashes. Sampling and selection decisions are reproducible from the frozen inputs and observations. Checks are recorded separately in checks.json.','');
writeFileSync(root+'README.md',lines.join('\n'));
console.log(`Proposed ${selected.length} headwords; protected inputs and ${runs.length} traces verified.`);
